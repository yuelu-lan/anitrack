spring:
  datasource:
    username: root
    password: your-local-mysql-password
  flyway:
    locations: classpath:db/migration,classpath:db/migration-dev

anitrack:
  jwt:
    secret: your-jwt-secret-base64-at-least-32-bytes
